# ==============================================
# Currency Exchange System - Environment Config
# ==============================================
# Copy this file to .env and fill in your values
# ⚠️ DO NOT commit .env file to git!

# Database Configuration
POSTGRES_DB=currency_exchange_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# JWT Configuration (Generate secure values for production!)
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
JWT_EXPIRATION=86400000

# Application Profile (dev, test, prod)
SPRING_PROFILES_ACTIVE=dev

# PgAdmin Configuration (Optional - use with: docker-compose --profile tools up)
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# ==============================================
# Production Setup:
# 1. Generate secure JWT_SECRET:
#    PowerShell: [Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
#    Linux/Mac: openssl rand -base64 32
#
# 2. Generate secure POSTGRES_PASSWORD:
#    PowerShell: [Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(24))
#    Linux/Mac: openssl rand -base64 24 | tr -d "=+/"
#
# 3. Set SPRING_PROFILES_ACTIVE=prod
# ==============================================
